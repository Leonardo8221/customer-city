import React from 'react';
import { Modal } from 'components/ui';
import { Resource } from '../../../JourneyBuilderProvider';

export default function ResourceEdit(props: { resource: Resource; show: boolean }) {
  return <>Config</>;
}
