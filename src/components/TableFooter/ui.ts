import { styled, Box } from '@mui/material';

export const Container = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'flex-end',
  alignItems: 'center',
  paddingTop: 16,
  height: 40,
}));
